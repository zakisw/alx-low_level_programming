#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
char _puts_recursion(char c);
{
	putchar(char c );
}
int main(void)
{
    _puts_recursion("Puts with recursion");
    return (0);
}
